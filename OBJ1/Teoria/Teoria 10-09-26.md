Herencia: mecanismo que permite a una clase heredar estructura y comportamiento de otra clase, estrategia de reusó de código.
solo Herencia simple en java
Característica Transitiva: forma en la que vemos la herencia. 
Ejemplo:
![[Pasted image 20260909150039.png]]

<font color="#00b050">Principio de sustitución de liskov: </font>si usamos una clase que es extendida, deberíamos poder usar cualquiera de sus sub-clases  y que el programa sea valido.
<font color="#00b050">Method lookup:</font> cuando un obj recibe un msj y lo busca en su clase un método que corresponda el msj.
Con Herencia: lo mismo pero si no lo encuentra busca en la superclase de subclase y así sucesivamente hasta encontrar o lanzar error.
<font color="#00b050">Overriding:</font> sobre escribir métodos, si una clase tiene un método con mismo nombre de una super, el de la super queda oculto y siempre que le manden el mensaje ejecuta su metodo. En ese método puedo hacer algo adicional además del heredado llamado igual. 
<font color="#00b050">Super:</font> como .this, hacen referencia al obj que lo ejecuta. Cual uso cambia forma del method lookup, se usa para extender comportamiento heredado.
<font color="#00b050">Super en constructores:</font> si quiero usar comport de otro construct lo invoco usando super. 
EJ:
	public cuentaBancaria(Persona titular){
		this.titular = titular; 
	}
	public CuentaCorriente(Persona titular, double saldoInicial, double limiteDescubierto) {
		super(titular);
		saldo = saldoInicial;
		this. limiteDescubierto = limiteDescubierto;
	}
<font color="#00b050">Clase abstracta: </font> clase que no tiene instancia, tiene variables y metodos. Su function es encapsular lo que es comun a sus subclases.
<font color="#00b050">Metodo Abstracto:</font> es un metodo no implementado que debe set implementado en sus subclases. es decir sub entienden pero no saben q have.
<font color="#00b050">Tipos/tipado en  OO:</font>  
	• Tipo = Conjunto de firmas de operaciones/métodos (nombre, tipos de argumentos) 
	• Cada clase en Java define un tipo 
	• Cada <font color="#c0504d">interfaz</font> en Java define un tipo 
	• Cuando una clase implementa una <font color="#c0504d">interfaz</font>, es un sub-tipo de ella 
	• Cuando una clase <font color="#c0504d">subclasifica</font> a otra, es un sub-tipo de ella 
	• El tipo de una <font color="#c0504d">variable</font>, es el que indico al declararla
	• El tipo de un <font color="#c0504d">método</font>, es el tipo de lo que retorna
	• El tipo de un <font color="#c0504d">constructor</font>, es la clase que construye
<font color="#00b050">Visibilidad:</font>
	Si declaro una variable de instancia como private en una clase A: 
		• Las instancias de las subclases de A tendrán esa variable de instancia 
		• En los métodos de las subclases de A, no puedo hacer referencia ella 
	• Si declaro un método “m()” como private en una clase A: 
		• Las instancias de sus subclases entenderán el mensaje m() 
		• En los métodos de las subclases de A no puedo enviar m() a ”this“ 
	• Si declaro variables y métodos como protected, puedo verlos en las subclases
<font color="#00b050">En OBJ1:</font>
• El ocultamiento favorece el bajo acoplamiento.
• Var instancia siempre privadas.
• Si en una subclase, para extender o especializar, dependo de ese conocimiento, cambio la visibilidad de mis variables a <font color="#c0504d">protected</font> (# en los diagrams de UML).

# Resumen: Herencia (OBJ1)

## 1. Conceptos Básicos
* **Herencia**: Mecanismo que permite a una clase heredar estructura (variables) y comportamiento (métodos) de otra clase. Es una estrategia de reúso de código y de conceptos[cite: 2].
* **Herencia simple en Java**: En Java solo tenemos herencia simple (una clase solo puede extender de una superclase).
* **Característica transitiva**: Si una clase `C` hereda de `B` y `B` hereda de `A`, entonces `C` también hereda de `A`.
* **Vocabulario básico**:
  * La clase hija es la **subclase** (hereda o extiende a otra)[cite: 2].
  * La clase padre es la **superclase**[cite: 2].
* **La prueba del "Es-un"**: Regla clave para saber si la herencia está bien aplicada[cite: 2]. Si suena natural en el dominio, es probable que esté bien (ej: una `CuentaCorriente` es una `CuentaBancaria`)[cite: 2]. Un contraejemplo: un `Circulo` NO es un `Punto` (el círculo *tiene* un punto de centro, usa composición y no herencia)[cite: 2].
* **Principio de sustitución de Liskov (LSP)**: Si usamos una clase que es extendida, deberíamos poder usar cualquiera de sus subclases y que el programa siga siendo válido (que sigan pasando los tests).

---

## 2. Búsqueda de Métodos (Method Lookup) y Overriding
* **Method Lookup**: Cuando un objeto recibe un mensaje, busca en su clase un método que corresponda con ese mensaje.
  * **Con herencia**: Si no lo encuentra en su clase, busca en la superclase de su clase, y así sucesivamente hacia arriba hasta encontrarlo o lanzar error. La búsqueda termina apenas encuentra la primera coincidencia[cite: 2].
* **Overriding (sobreescribir métodos)**: 
  * Ocurre si una subclase tiene un método con el mismo nombre y parámetros que uno de la superclase.
  * El método de la superclase queda **oculto** y siempre que le manden el mensaje al objeto ejecuta el método de la subclase.
  * En ese método puedo hacer algo adicional además del comportamiento heredado.

---

## 3. `this` vs `super`
* **Similitud**: Tanto `super` como `this` funcionan como pseudo-variables y hacen referencia al **mismo objeto** que ejecuta el método (al receptor).
* **Diferencia**: Usar `super` cambia la forma en la que se hace el *method lookup*.
  * `this`: Inicia la búsqueda del método desde la clase real del objeto receptor.
  * `super`: Inicia la búsqueda a partir de la superclase directa. Se usa exclusivamente para **extender** comportamiento heredado.

---

## 4. `super` en Constructores
* Los constructores en Java **no se heredan**[cite: 2].
* Si quiero usar el comportamiento de inicialización del constructor padre, lo invoco usando `super(...)`.
* **Regla obligatoria**: La llamada a `super(...)` debe ser la **primera sentencia** en el constructor de la subclase[cite: 2].

```java
public CuentaBancaria(Persona titular) {
    this.titular = titular;
}

public CuentaCorriente(Persona titular, double saldoInicial, double limiteDescubierto) {
    super(titular); // Debe ser la primera línea
    this.saldo = saldoInicial;
    this.limiteDescubierto = limiteDescubierto;[cite: 1, 2]
}
